---
import Layout from "../layouts/Layout.astro";
import { timelineEvents } from "src/util/constants";

timelineEvents.sort((a, b) => (a.dateFrom > b.dateFrom ? -1 : 1));
---

<Layout title="About Me">
  <main class="min-h-screen bg-base-200">
    <div class="max-w-5xl mx-auto">
      <div class="grid gap-4 mx-4 grid-cols-12">
        <div class="col-span-12">
          <div
            class="mt-8 text-center sm:text-left mb-14 flex flex-col items-center"
          >
            <h3 class="text-3xl font-semibold">
              <span class="text-accent">Hello</span> Again
            </h3>
            <p
              class="text-sm font-bold tracking-wider text-base-content w-3/5 mt-4"
            >
              I've lived many lives and worn many different hats. My comfort
              stack is JavaScript on the front- and back-end, but I'm also
              comfortable with Java, Python, and Ruby on Rails. I would love a
              chance to learn Elixir.
            </p>
            <p
              class="text-sm mt-4 font-bold tracking-wider text-base-content w-3/5"
            >
              I also enjoy data engineering, and have experience using Airflow
              and Databricks to build and run ETL workflows.
            </p>
          </div>
        </div>
        <div class="relative col-span-12 px-4 space-y-6 sm:col-span-9">
          <div
            class="col-span-12 space-y-12 relative px-4 sm:col-span-8 sm:space-y-8 sm:before:absolute sm:before:top-2 sm:before:bottom-0 sm:before:w-0.5 sm:before:-left-3 before:bg-neutral"
          >
            {
              timelineEvents.map((event, idx) => (
                <div class="flex flex-col sm:relative sm:before:absolute sm:before:top-2 sm:before:w-4 sm:before:h-4 sm:before:rounded-full sm:before:left-[-35px] sm:before:z-[1] before:bg-neutral">
                  <h3 class="text-xl font-semibold tracking-wide">
                    {event.role ? event.role + " @ " : ""} {event.title}
                  </h3>
                  <time class="text-xs tracking-wide uppercase text-secondary">
                    {event.location} {event.dateFrom} &mdash;
                    {event.dateTo || "Present"}
                  </time>
                  <p class="mt-3 text-base-content">{event.description}</p>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>
